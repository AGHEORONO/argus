# Imagery and rasters — heavy, never pushed
data/
*.tif
*.tiff
*.las
*.laz

# Python
.venv/
__pycache__/
*.pyc

# Node
node_modules/
dist/

# Playwright — rapoarte, urme si capturi de la rulari esuate
app/frontend/test-results/
app/frontend/playwright-report/
app/frontend/blob-report/

# Configurari de la alte unelte de agent, per-masina
.cursor/
.kimi-code/
opencode.json

# Aplicatia de desktop — artefacte de build, ~300MB
dist-desktop/
build-desktop/
Argus-Custode-windows.zip

# Canvasul de design: sursele se pastreaza, pagina asamblata (~2MB de editor) nu
design/*.html
!design/*.dc.html

# Obsidian — per-machine UI state, causes conflicts between PC and laptop
CodeVault/.obsidian/workspace.json
CodeVault/.obsidian/workspace-mobile.json
CodeVault/.trash/

# OS
Thumbs.db
.DS_Store
.vercel
.env*
